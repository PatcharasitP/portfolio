# DESIGN ของ Portfolio

ระบบดีไซน์ของเว็บนี้ อ่านไฟล์นี้ก่อนแก้หน้าจอทุกครั้ง (ทั้งคนและ AI) กฎในหมวด 1 มีเทสบังคับ แดง = ห้ามปล่อยขึ้นเว็บ
ค่าจริงของทุก token อยู่ใน `:root` ของ `index.html` ไฟล์นี้สรุปไว้ให้อ่านเร็ว ถ้าไม่ตรงกัน ให้ถือ `index.html` เป็นหลักแล้วแก้ไฟล์นี้ตาม

## 1. กฎเหล็ก (มีเทสบังคับ)
| กฎ | เทสที่บังคับ |
|---|---|
| ห้ามใช้จุดกลาง ขีดยาว และขีดสั้น (U+00B7 U+2014 U+2013) ในข้อความที่คนเห็น รวมตัวที่ CSS วาดเอง (`::before` `::after`) ใช้จุลภาคหรือเว้นวรรคแทน | `tests/browser_separators.py`, `tests/browser_gates.py` |
| ปุ่มและลิงก์ต้องอยู่บรรทัดเดียวทุกจอ 320 ถึง 1920 px ข้อความยาวให้มีข้อความสั้นสำหรับจอแคบ | `tests/browser_gates.py` |
| หัวข้อภาษาไทย line-height ไม่ต่ำกว่า 1.3 เนื้อความ 1.75 (สระและวรรณยุกต์ล้นกล่อง) | `tests/browser_gates.py` |
| ข้อความไทยถ่างตัวอักษรได้ไม่เกิน 0.04em (เกินนั้นคำแตกออกจากกัน) ภาษาอังกฤษตัวพิมพ์ใหญ่ถ่างได้ | `tests/browser_gates.py` |
| คอนทราสต์ผ่าน WCAG 2.1 AA ทั้งธีมสว่างและมืด และไม่มีหน้าไหนเลื่อนแนวนอน | `tests/browser_gates.py` |
| ฟอนต์ Sarabun โหลดจาก `assets/fonts` ของเราเอง น้ำหนัก 400 600 700 เท่านั้น เพิ่มสัญลักษณ์ใหม่ให้รัน `tools/build-fonts.py` | `tests/browser_fonts.py` |
| ผู้ใช้ตั้งลดการเคลื่อนไหว หน้าต้องนิ่ง | `tests/browser_page.py` |

รันครบก่อนขึ้นเว็บ: `python3 tests/browser_page.py && python3 tests/browser_separators.py && python3 tests/browser_fonts.py && python3 tests/browser_gates.py`
(`browser_gates.py` ใช้ตัววัดกลาง `Scripts/ui_gates` ของ workspace)

## 2. Token
- **สี (สว่าง):** พื้น `#fbfbfa`, การ์ด `#ffffff`, พื้นรอง `#f4f4f2`, เส้น `#e3e3df` และ `#ececeb`, ตัวอักษร `#1a1a18` `#4a4a46` `#6b6b64`, สีเน้น `#1d4ed8` พื้นเน้น `#eef2ff`, สีผ่าน `#0f7b4f`
- **สี (มืด):** พื้น `#141414`, การ์ด `#1c1c1b`, พื้นรอง `#232322`, เส้น `#33332f` และ `#2a2a28`, ตัวอักษร `#e6e6e1` `#b4b4ad` `#8a8a82`, สีเน้น `#93b4ff` พื้นเน้น `#1e2438`, สีผ่าน `#5ed6a0` (ไม่ใช้ดำหรือขาวสนิท)
- **ฟอนต์:** เนื้อหา Sarabun (ฟอนต์สำรองตอนกำลังโหลดคือ Leelawadee UI ที่ปรับขนาดให้ใกล้กัน), ป้ายเล็กและตัวเลขใช้ฟอนต์ตัวพิมพ์ดีดของเครื่อง
- **ขนาด:** ตัวอักษรฐาน 17px อัตราส่วน 1.25, หัวเว็บ 32 ถึง 50px, หัวข้อส่วน 19 ถึง 23px
- **มุมโค้ง:** การ์ด 14px, ย่อย 9px, ปุ่มและชิปโค้งเต็ม 999px
- **ความกว้างเนื้อหา:** 920px, เนื้อความยาวไม่เกิน 70ch

## 3. ท่าของปุ่ม
- ปุ่มหลัก `.btn` พื้นสีตัวอักษร ตัวหนังสือสีพื้น หนึ่งจุดต่อหนึ่งส่วนเท่านั้น
- ปุ่มรอง `.btn.alt` พื้นการ์ด ขอบบาง, ปุ่มเล็กบนแถบ `.ghost`, ลิงก์ในการ์ด `.chip`
- ปุ่มในแถวเดียวกันสูงเท่ากัน (ทุกแบบมีขอบ 1px เท่ากัน)
- สัญลักษณ์ที่ Sarabun ไม่มี (✉ ☀ ☾ ↗) มาจากฟอนต์ของเครื่อง ถ้าจะเพิ่มใหม่ให้ใช้ SVG

## 4. หลักรสนิยมของเจ้าของเว็บ (จากคำพูดจริง)
- **โปร่ง บาง จาง** ชนะ หนา ทึบ หลายสี, ใช้สีเน้นสีเดียว
- **กลัวรก:** ของที่พับได้ก็ยังรก ไม่เพิ่มแผงหรือคอลัมน์ข้าง ถ้าไม่จำเป็นให้ตัดออก
- **ข้อความบนจอเอาแค่คำหลัก** คำอธิบายที่อ่านแล้วต้องคิดต่อให้ตัดทิ้ง
- **เปลี่ยนเรื่องหน้าตาต้องทำเป็นตัวเลือกให้เจ้าของเทียบภาพก่อน** ไม่เปลี่ยนแล้วค่อยบอก

## 5. เรื่องที่เครื่องมือตรวจดีไซน์ทั่วไปมักทัก แต่เว็บนี้ตั้งใจทำแบบนี้ (ห้ามแก้โดยไม่ถาม)
- พื้นการ์ดขาวสนิท `#ffffff` บนพื้น `#fbfbfa`
- หัวเว็บระยะบนมากกว่าระยะล่าง เพราะแถวตัวเลขสรุปต่อท้ายหัวเว็บทันที
- ส่วนเนื้อหาโผล่ขึ้นเบา ๆ ตอนเลื่อนถึง (ปิดเองเมื่อตั้งลดการเคลื่อนไหว)
- ป้ายเล็กตัวพิมพ์ใหญ่ถ่างตัวอักษรในภาษาอังกฤษ
